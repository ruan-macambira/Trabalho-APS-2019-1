<h1>Página Inicial</h1>
Últimos Eventos Cadastrados:
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th><%= Event.human_attribute_name :name %></th>
      <th><%= t 'labels.models.event.submission_period' %></th>
    </tr>
  </thead>
  <tbody>
    <% @latest_events.each do |event| %>
    <tr>
      <td><%=link_to event.name, event_path(event.id)%></td>
      <td><%=l event.submission_start%> a <%=l event.submission_finish%></td>
    </tr>
    <%end%>
  </tbody>
</table>
<%=link_to "Listar Todos", events_path, class: 'btn btn-primary'%>